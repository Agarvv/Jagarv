export interface Filters {
        category: string;
        minPrice: number | null;
        maxPrice: number | null;
        ram: string | null;
        storage: string | null;
        color: string | null;
}